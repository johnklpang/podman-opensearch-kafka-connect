# Lab defaults — securityDashboards is removed at image build time
# (matches OpenSearch DISABLE_SECURITY_PLUGIN=true).
server.host: "0.0.0.0"
server.port: 5601
opensearch.hosts: ["http://opensearch:9200"]
opensearch.ssl.verificationMode: none
logging.dest: stdout
logging.silent: false
